.container
  .row
    .col-md-offset-4.col-md-4
      %p We are almost done, lets create your profile so you can log in to your portal's main page, Please choose a username and password below:
      .panel.panel-default
        .panel-body
          = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
            = f.error_notification
            = f.input :name
            = f.input :email
            = f.input :password, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length)
            = f.input :password_confirmation
            .text-center
              = button_tag 'Sign Up Now', type: :submit, class: 'btn btn-primary'
